# SQL Server host (Docker uses localhost,1433).
DB_SERVER=localhost,1433

# Database name created/used by Entity Framework migrations.
DB_NAME=PeopleHubDB

# SQL Server user (sa for local Docker).
DB_USER=sa

# Local password (Do not commit real secrets to source control.)
DB_PASSWORD=PeopleHub@2026
